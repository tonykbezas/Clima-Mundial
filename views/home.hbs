{{> header }}

   {{> navbar }}

    <div class="row">
    <div class="col-sm-6">
        <div class="card">
        <div class="card-body">
            <h5 class="card-title">{{ ciu1 }}</h5>
            <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            <a href="#" class="btn btn-primary">{{ cli2 }}</a>
        </div>
        </div>
    </div>
    <div class="col-sm-6">
        <div class="card">
        <div class="card-body">
            <h5 class="card-title"> {{ ciu2 }} </h5>
            <p class="card-text"> {{ cli2 }} </p>
            <a href="#" class="btn btn-primary">{{ cli2 }}</a>
        </div>
        </div>
    </div>
    </div>

{{> footer }}
   